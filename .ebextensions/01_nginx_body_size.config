files:
  "/etc/nginx/conf.d/99_client_max_body_size.conf":
    mode: "000644"
    owner: root
    group: root
    content: |
      client_max_body_size 8m;

container_commands:
  01_reload_nginx:
    command: "nginx -t && (systemctl reload nginx || service nginx reload) || true"
